{
  "name": "default",
  "description": "Template profile; replace checks with your repo validators.",
  "phases": [
    {
      "name": "structural",
      "checks": [
        {
          "name": "example.schema",
          "command": ["python", "-c", "print('replace me')"],
          "cwd": "{repo_root}",
          "severity": "error"
        }
      ]
    }
  ]
}

