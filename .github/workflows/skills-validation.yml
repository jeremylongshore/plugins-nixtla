name: Claude Skills Validation (Strict)

on:
  push:
    branches: ["main", "master", "**"]
  pull_request:

jobs:
  validate-skills:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dev dependencies
        run: |
          if [ -f requirements-dev.txt ]; then
            pip install -r requirements-dev.txt
          else
            pip install pyyaml
          fi

      - name: Validate Claude Skills (v7.0 marketplace tier, min-grade B)
        run: |
          python 004-scripts/validate_skills_v2.py --marketplace --min-grade B --show-low-grades
